name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    name: Build and release UniManager
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: lts/*

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.7.1'

      - name: Install semantic-release
        run: >-
          npm install --no-save
          semantic-release
          @semantic-release/commit-analyzer
          @semantic-release/release-notes-generator
          @semantic-release/github

      - name: Determine semantic release version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          release_output="$(npx semantic-release --dry-run 2>&1)"
          release_status=$?
          set -e
          printf '%s\n' "$release_output"
          release_version="$(printf '%s\n' "$release_output" | sed -nE 's/.*next release version is ([0-9]+\.[0-9]+\.[0-9]+).*/\1/p' | tail -n 1)"
          if [ -z "$release_version" ]; then
            release_version="0.1.0"
          fi
          test -n "$release_version" || {
            echo "Could not determine UniManager version" >&2
            exit 1
          }
          echo "UNIMANAGER_VERSION=$release_version" >> "$GITHUB_ENV"
          echo "Using UniManager version $release_version (semantic-release dry-run status: $release_status)"

      - name: Prepare release keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.UNIMANAGER_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.UNIMANAGER_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.UNIMANAGER_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.UNIMANAGER_KEY_PASSWORD }}
        run: |
          test -n "$KEYSTORE_BASE64" || {
            echo "UNIMANAGER_KEYSTORE_BASE64 is not configured" >&2
            exit 1
          }
          test -n "$KEYSTORE_PASSWORD" || {
            echo "UNIMANAGER_KEYSTORE_PASSWORD is not configured" >&2
            exit 1
          }
          test -n "$KEY_ALIAS" || {
            echo "UNIMANAGER_KEY_ALIAS is not configured" >&2
            exit 1
          }
          test -n "$KEY_PASSWORD" || {
            echo "UNIMANAGER_KEY_PASSWORD is not configured" >&2
            exit 1
          }
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/unimanager-release.jks"

      - name: Build release APK
        run: gradle assembleRelease --no-daemon
        env:
          UNIMANAGER_KEYSTORE_FILE: ${{ runner.temp }}/unimanager-release.jks
          UNIMANAGER_KEYSTORE_PASSWORD: ${{ secrets.UNIMANAGER_KEYSTORE_PASSWORD }}
          UNIMANAGER_KEY_ALIAS: ${{ secrets.UNIMANAGER_KEY_ALIAS }}
          UNIMANAGER_KEY_PASSWORD: ${{ secrets.UNIMANAGER_KEY_PASSWORD }}

      - name: Name release APK
        run: cp app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/UniManager.apk

      - name: Upload APK workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: UniManager-apk
          path: app/build/outputs/apk/release/UniManager.apk
          if-no-files-found: error

      - name: Run semantic-release
        uses: cycjimmy/semantic-release-action@v6
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK to GitHub release
        if: steps.release.outputs.new_release_published == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          gh release upload
          "${{ steps.release.outputs.new_release_git_tag }}"
          app/build/outputs/apk/release/UniManager.apk
          --clobber
